class LocalSearch {
    constructor(trackSettings) {
        this.trackSettings = trackSettings
    }

    /**
     * [NOT IMPLEMENTED]
     * Tries to optimize the given solution.
     * 
     * First tries to fit all excess talks into the existing tracks,
     * then tries to reduce the amount of tracks.
     * @returns unaltered solution (for now).
     */
    optimize(solution) {
        // TODO: implement
        return solution
    }
}


module.exports = LocalSearch